diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index d0cf74d..02da539 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -41,7 +41,13 @@ jobs:
         run: |
           python -m pip install --upgrade pip
           pip install -r requirements.txt
-          pip install pytest
+          pip install pytest mypy
+
+      # Ratchet, not a gate on existing debt: mypy.ini exempts the modules that
+      # already carry errors and checks everything else, so new modules and new
+      # regressions in clean ones fail here. Shrink the exemption list over time.
+      - name: Type check
+        run: python -m mypy
 
       - name: Run tests
         env:
